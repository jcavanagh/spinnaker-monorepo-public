name: Gate
run-name: Gate ${{ github.ref_name }} (${{ github.event_name }})

on:
  workflow_dispatch:
    inputs:
      skip-tests:
        description: If specified, does not run tests
        type: boolean
        default: false


  push:
    branches:
      - main
      - release-*
    paths:
      - 'gate/**'
      - 'fiat/**'
      - 'kork/**'

  pull_request:
    branches:
      - main
      - release-*
    paths:
      - 'gate/**'
      - 'fiat/**'
      - 'kork/**'

jobs:
  gate:
    uses: ./.github/workflows/generic-build-publish.yml
    with:
      project: gate
      publish: ${{ github.event_name != 'pull_request' }}
    secrets: inherit
